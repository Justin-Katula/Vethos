appId: com.nexus.blocking
productName: Vethos
directories:
  output: release
  buildResources: build
icon: build/icon.ico

files:
  - 'out/**/*'
  - 'package.json'
  - '!**/.vscode/*'
  - '!**/{.DS_Store,.git,.gitkeep,.gitignore,.npmrc,.eslintrc.cjs,.prettierrc}'
  - '!**/{README.md,docs}'

asar: true

win:
  target:
    - target: nsis
      arch:
        - x64
    - target: portable
      arch:
        - x64

nsis:
  artifactName: ${productName}-Setup-${version}.${ext}
  oneClick: false
  perMachine: true
  allowElevation: true
  allowToChangeInstallationDirectory: false
  guid: a8e0b9da-61a1-4be1-9c1e-6ad8d7994e0f
  deleteAppDataOnUninstall: false
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: Vethos
  installerIcon: build/installer-icon.png
  uninstallerIcon: build/installer-icon.png
  installerHeaderIcon: build/installer-icon.png

portable:
  artifactName: ${productName}-Portable-${version}.${ext}

publish:
  provider: github
  owner: obedi
  repo: Nexus
